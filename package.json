{
  "name": "polyagent-mcp-services",
  "version": "1.0.0",
  "description": "MCP服务依赖包管理",
  "scripts": {
    "install-mcp": "npm install -g @alipay/mcp-server-alipay @paypal/mcp",
    "setup": "npm run install-mcp"
  },
  "dependencies": {
    "@alipay/mcp-server-alipay": "^1.4.0",
    "@paypal/mcp": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.30"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
